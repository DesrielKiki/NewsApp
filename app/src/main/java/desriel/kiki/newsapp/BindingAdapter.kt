package desriel.kiki.newsapp

import android.util.Log
import android.widget.RelativeLayout
import androidx.databinding.BindingAdapter
import androidx.navigation.findNavController

class BindingAdapter {
/*    companion object{
        @BindingAdapter("android:navigateToNewsDetail")
        @JvmStatic
        fun navigateToNewsDetail(view: RelativeLayout, newsId: String?){
            view.setOnClickListener {
                val action = HomeFragmentDirections.actionHomeFragmentToDetailFragment(newsId)
                view.findNavController().navigate(action)
                Log.d("binding adapter", "news id send to detail = $newsId")
            }
        }
    }*/
}